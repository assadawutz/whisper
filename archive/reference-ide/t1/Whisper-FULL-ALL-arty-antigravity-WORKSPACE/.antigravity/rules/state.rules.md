# State Rules (No white-screen)

- Always: loading / empty / error
- Guard lists: Array.isArray(list) && list.length > 0
- Optional chaining for deep props
- Avoid derived state duplication unless memoized and justified
